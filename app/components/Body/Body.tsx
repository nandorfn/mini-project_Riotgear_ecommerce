import ScrollMenu from "../Menu/ScrollMenu";
import ScrollMenuContainer from "../Menu/ScrollMenuContainer";

const Body: React.FC = () => {
    return (
        <>
          <ScrollMenuContainer />
        </>
    );
};

export default Body;