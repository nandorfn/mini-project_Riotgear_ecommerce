import { Inter, Bebas_Neue, Rubik } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
})
export const rubik = Rubik({
  subsets: ['latin'],
  display: 'swap',
})

export const bebas_neue = Bebas_Neue({
  subsets: ['latin'],
  weight: '400',
  display: 'swap',
})